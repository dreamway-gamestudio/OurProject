using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlyingTextPrefab : MonoBehaviour
{
    public GameObject _FlyingTextPrefab;
    void ShowFlyingText()
    {/*
        var go = Instantiate(_FlyingTextPrefab, UpdatePosition, Quaternion.identity, transform);
        go.GetComponent<TMPro.TextMeshPro>().text = EnemyMove.damage.ToString();
        */
    }
}
